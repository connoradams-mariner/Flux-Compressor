// No extra sbt plugins are required today.  This file is kept so
// sbt picks up the `project/` directory layout cleanly and to reserve
// a spot for publish / assembly plugins once the connector is ready to
// release (e.g. `sbt-assembly` for shaded fat JARs).
